export class User {
    id!: number | null; //User id
    username!: string | null; //User name
    name!: string | null; //User full name
    address!: string | null; //User rol
    phone!: string | null; //User rol
    email!: string | null; //User email
  }